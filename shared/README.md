# shared/

This directory will contain code, types, constants, and utilities shared across both the `app/` and `backend/` layers.

## Intended Scope

- Shared data models and type definitions
- Common validation logic
- Shared constants (e.g., drill categories, scoring rules)

## Current Status

**Empty — Foundation stage only.** No shared code will be placed here until the architecture is defined in `docs/architecture/`.
