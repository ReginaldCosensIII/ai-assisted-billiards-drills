# tooling/

This directory will contain build tool configuration, linter setups, and developer experience utilities.

## Intended Scope

- Compiler and bundler configuration
- Code formatting and linting rule sets
- Pre-commit hook configurations
- Developer environment toolchain definitions

## Current Status

**Empty — Foundation stage only.** Tooling decisions will follow framework selection, which will be documented in `docs/architecture/`.
